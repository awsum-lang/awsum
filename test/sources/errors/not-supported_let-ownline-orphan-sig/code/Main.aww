{- An own-line `let` signature must be followed by its own definition.
   Here `res : Int32` is followed by a different binder, so the signature
   is orphaned — rejected at parse time. -}

import IO.Stdout

main : IO Never Unit
main =
  let res : Int32
      other = 2
   in IO.Stdout.print "x"
