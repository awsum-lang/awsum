-- Error: Int32 literal 1 below lower bound (-2^31 - 1)
import IO.Stdout

main : Either (StringTooLong | UnpairedUtf16Surrogate) String -> IO Unit
main _ = IO.Stdout.print "ok"

underMin : Int32
underMin = -2147483649
