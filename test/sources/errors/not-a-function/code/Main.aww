-- Error: applying a String value as if it were a function
import IO.Stdout

main : Either (StringTooLong | UnpairedUtf16Surrogate) String -> IO Unit
main rawArg = case rawArg of
  Left _ -> IO.Stdout.print "x"
  Right input -> input input
