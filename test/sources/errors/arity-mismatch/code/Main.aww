-- Error: function defined with more parameters than its signature allows
import IO.Stdout

foo : String -> IO Unit
foo a b = IO.Stdout.print a

main : String -> IO Unit
main input = foo input "extra"
