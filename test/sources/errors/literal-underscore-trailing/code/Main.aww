-- Trailing '_' in an integer literal is rejected at parse time.
-- '_' is allowed only between digits — `1_` has nothing on the
-- right and is a syntax error. Workaround: drop the trailing '_'.
import IO.Stdout

bad : Int32
bad = 1_

main : IO Never Unit
main = IO.Stdout.print "ok"
