-- Not yet supported: Haskell's `$` low-precedence application
-- operator. There are no user-facing operators in Awsum besides the
-- single carved-out `(++)`. Workaround: write the application with
-- explicit parens.
import IO.Stdout

main : IO Never Unit
main = IO.Stdout.print $ "x"
