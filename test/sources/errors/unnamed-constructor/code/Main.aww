-- Error: bare '_' is not a valid constructor name. Constructors are
-- referenced from expressions and patterns, so even unused ones must be
-- nameable: use '_C' (or similar).
import IO.Stdout

type A = _

main : Either (StringTooLong | UnpairedUtf16Surrogate) String -> IO Never Unit
main input = IO.Stdout.print input
