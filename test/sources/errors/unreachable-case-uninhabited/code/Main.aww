{- Error: pattern on uninhabited constructor (Err Never is unreachable) -}

import IO.Stdout

type Result a e = Ok a | Err e

unwrap : Result String Never -> String
unwrap r = case r of
  Ok value -> value
  Err value -> ""

main : IO Never Unit
main = IO.Stdout.print (unwrap (Ok "hello"))
