-- Error: the 'x' in `Box x` shadows the outer function parameter 'x'.
import IO.Stdout

type Box a = Box a

unwrap : Box String -> String
unwrap x = case x of
  Box x -> x

main : Either (StringTooLong | UnpairedUtf16Surrogate) String -> IO Unit
main input = IO.Stdout.print (unwrap (Box "hello"))
