-- Error: BuiltIn.nonexistent is not provided by the compiler.
import IO.Stdout

main : Either (StringTooLong | UnpairedUtf16Surrogate) String -> IO Unit
main _ = IO.Stdout.print "ok"

bogus : String
bogus = BuiltIn.nonexistent
