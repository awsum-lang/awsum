{- Non-parametric single-constructor wrapper over a concrete field. -}
import IO.Stdout

type Wrapper = Wrap String

unwrap : Wrapper -> String
unwrap w = case w of
  Wrap value -> value

main : IO Never Unit
main = IO.Stdout.print (unwrap (Wrap "hello"))
