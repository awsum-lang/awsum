{- Composes two IO actions with `andThenIO`: the first prints "a", its
   continuation prints "b". The `|>` form puts the actions in execution
   order left-to-right. Prints `ab`. -}

import IO.Stdout

main : IO Never Unit
main = IO.Stdout.print "a" |> andThenIO (\_u -> IO.Stdout.print "b")
