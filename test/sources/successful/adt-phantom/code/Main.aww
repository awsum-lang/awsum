-- Test: phantom type parameter (unused in constructor) compiles and runs correctly
import IO.Stdout

type Phantom _tag = Phantom

show : Phantom String -> String
show p = case p of
  Phantom -> "Phantom"

main : String -> IOUnit
main _input = IO.Stdout.print (show Phantom)
