{- Phantom type parameter (unused in any constructor). -}
import IO.Stdout

type Phantom _tag = Phantom

show : Phantom String -> String
show p = case p of
  Phantom -> "Phantom"

main : IO Never Unit
main = IO.Stdout.print (show Phantom)
