{- Parametric single-constructor type; pattern match unwraps. -}
import IO.Stdout

type Box a = Box a

unwrap : Box String -> String
unwrap b = case b of
  Box value -> value

main : IO Never Unit
main = IO.Stdout.print (unwrap (Box "hello"))
