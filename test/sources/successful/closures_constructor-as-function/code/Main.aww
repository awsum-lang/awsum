{- Constructor passed as a first-class function argument to a HOF. -}
import IO.Stdout

type Box a = Box a

wrap : (String -> Box String) -> String -> Box String
wrap f x = f x

unwrap : Box String -> String
unwrap b = case b of
  Box value -> value

main : IO Never Unit
main = IO.Stdout.print (unwrap (wrap Box "wrapped"))
