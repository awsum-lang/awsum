-- Test: single nullary constructor (Unit type) with pattern matching
import IO.Stdout

type Unit = Unit

show : Unit -> String
show u = case u of
  Unit -> "Unit"

main : String -> IOUnit
main _input = IO.Stdout.print (show Unit)
