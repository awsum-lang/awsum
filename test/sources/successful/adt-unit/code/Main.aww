-- Test: single nullary constructor (CustomUnit type) with pattern matching
import IO.Stdout

type CustomUnit = CustomUnit

show : CustomUnit -> String
show u = case u of
  CustomUnit -> "CustomUnit"

main : String -> IOUnit
main _input = IO.Stdout.print (show CustomUnit)
