{- `pureIO` lifts a pure value into IO, composed with `bindIO` whose
   continuation is the platform built-in `IO.Stdout.print` passed as a
   value. Prints `from pureIO`. -}

import IO.Stdout

main : IO Never Unit
main = bindIO (pureIO "from pureIO") IO.Stdout.print
