{- A platform-effect built-in (`IO.Stdout.print`) passed as a value into
   a higher-order combinator (`bindIO`). Prints `hello`. -}

import IO.Stdout

main : IO Never Unit
main = bindIO (pureIO "hello") IO.Stdout.print
