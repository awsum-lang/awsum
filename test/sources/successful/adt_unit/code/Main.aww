{- Single nullary constructor; pattern match. -}

import IO.Stdout

type CustomUnit = CustomUnit

show : CustomUnit -> String
show u = case u of
  CustomUnit -> "CustomUnit"

main : IO Never Unit
main = IO.Stdout.print (show CustomUnit)
