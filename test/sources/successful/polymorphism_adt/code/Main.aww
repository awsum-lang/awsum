{- Polymorphic function applied to a parametric ADT value. -}
import IO.Stdout

type Box a = Box a

identity : a -> a
identity x = x

main : Either (StringTooLong | UnpairedUtf16Surrogate) String -> IO Unit
main _input = IO.Stdout.print (case identity (Box "one") of
  Box v -> v)
