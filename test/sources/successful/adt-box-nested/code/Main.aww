-- Test: nested Box with inline nested pattern matching (Box (Box (Box value)))
import IO.Stdout

type Box a = Box a

unwrap : Box (Box (Box String)) -> String
unwrap b = case b of
  Box (Box (Box value)) -> value

main : String -> IOUnit
main _input = IO.Stdout.print (unwrap (Box (Box (Box "hello"))))
