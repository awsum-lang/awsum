{- Direct test of `showInvalidUtf8`: renders the sentinel returned by
   `IO.Stdin.readAllString` on input that is not well-formed UTF-8. -}

import IO.Stdout

main : IO Never Unit
main = IO.Stdout.print (showInvalidUtf8 InvalidUtf8)
