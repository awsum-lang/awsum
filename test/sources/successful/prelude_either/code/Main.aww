{- `Either a b` from prelude (no import). -}
import IO.Stdout

unwrap : Either String String -> String
unwrap r = case r of
  Left e -> "left: " ++ e
  Right v -> "right: " ++ v

main : String -> IO Unit
main _input = IO.Stdout.print (unwrap (Left "bad") ++ ", " ++ unwrap (Right "good"))
