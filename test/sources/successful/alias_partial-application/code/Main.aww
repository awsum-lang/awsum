{- An alias-form definition whose body is a partial application
   (`const "RESULT"`, supplying one of `const`'s two arguments). Applying
   `partial "ignored"` discards its argument. Prints `RESULT`. -}

import IO.Stdout

partial : String -> String
partial = const "RESULT"

main : IO Never Unit
main = IO.Stdout.print (partial "ignored")
